# packages/ui — дизайн-система (наполняется по фазам)

Токены «Оксид» пока живут прямо в web:
[`apps/web/app/globals.css`](../../apps/web/app/globals.css) (OKLCH-источник) и
[`apps/web/tailwind.config.ts`](../../apps/web/tailwind.config.ts).

Когда появится второй потребитель (admin, Фаза 5) — токены и общие компоненты выносятся сюда.
Правила — в [`docs/design-system.md`](../../docs/design-system.md).
